== Enunciado

¿Se pueden dar cotas mejores que funcionen a partir de algún $n_0$? Es decir, ¿existe $c(n) < 1 + ((n − 1)(n − 2))/2 ("resp." c(n) < 2 + ((n − 1)(n − 2))/2)$ tal que todo grafo de $n ≥ n_0$ vértices que tenga al menos $c(n)$ aristas sea conexo (resp. biconexo)?

